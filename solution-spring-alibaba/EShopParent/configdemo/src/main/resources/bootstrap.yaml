spring:
  application:
    name: configdemo
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        extension-configs[0]:
          data_id: redis.yaml
          group: DEFAULT_GROUP
          refresh: true
        extension-configs[1]:
          data_id: mq.yaml
          group: DEFAULT_GROUP
          refresh: true
  profiles:
    active: prod