server:
  port: 8089
spring:
  application:
    name: service-sentinel
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8080
management:
  endpoints:
    web:
      exposure:
        include: '*'